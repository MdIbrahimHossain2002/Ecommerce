# ShopHub Backend (Laravel API)

Laravel 10 REST API with Sanctum authentication.

## Commands

```bash
composer install
php artisan migrate
php artisan db:seed
php artisan serve
```

API base URL: `http://127.0.0.1:8000/api`
