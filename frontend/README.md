# ShopHub Frontend (React)

React + Vite + Tailwind CSS storefront and admin panel.

## Commands

```bash
npm install
npm run dev
```

App URL: `http://localhost:5173`

Requires the backend API running at `http://127.0.0.1:8000`.
